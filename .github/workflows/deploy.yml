name: Deploy z-player to EC2

on:
  push:
    branches:
      - tests/updates

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          # Надежная настройка SSH директории
          SSH_DIR="${HOME}/.ssh"
          mkdir -p "$SSH_DIR"
          chmod 700 "$SSH_DIR"

          # Добавляем приватный ключ
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > "$SSH_DIR/id_ed25519"
          chmod 600 "$SSH_DIR/id_ed25519"

          # Проверяем и чистим хост
          HOST=$(echo "${{ secrets.SSH_HOST }}" | tr -d '\r\n')
          if [ -z "$HOST" ]; then
            echo "SSH_HOST is empty, exiting"
            exit 1
          fi

          # Добавляем known_hosts
          ssh-keyscan -H "$HOST" >> "$SSH_DIR/known_hosts"

      - name: Deploy on server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'EOF'
            set -e

            echo "➡️ Go to project"
            cd /root/z-player

            echo "➡️ Pull latest code"
            git fetch origin
            git reset --hard origin/tests/updates

            echo "➡️ Stop old container if exists"
            docker stop z-player || true
            docker rm z-player || true

            echo "➡️ Build docker image"
            docker build -t z-player .

            echo "➡️ Run container"
            docker run -d \
              --restart unless-stopped \
              --name z-player \
              -p 3001:3001 \
              z-player

            echo "✅ Deploy finished"
          EOF
